
default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: 5

development:
  <<: *default
  database: islap_development

test:
  <<: *default
  database: islap_test

#
production:
  <<: *default
  database: islap_production
  username: isla
  password: <%= ENV['HOUSE_DATABASE_PASSWORD'] %>
